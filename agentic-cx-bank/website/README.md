
  # Latam Banco Mockup

  This is a code bundle for the Latam Banco mockup site.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.

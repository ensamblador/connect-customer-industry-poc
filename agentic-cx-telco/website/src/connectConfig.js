// connectConfig: configuración estática del sitio Latam Telco.
//
// El widget de Amazon Connect se inicializa directamente en index.html con
// valores hardcodeados. Este módulo solo expone el email por defecto que
// se usa como atributo de contacto cuando el usuario no ha iniciado sesión.

export const connectConfig = {
  defaultEmail: 'example@example.com',
};

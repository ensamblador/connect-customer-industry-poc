# Pinned so `cdk synth` output schema matches the CDK CLI used to deploy.
# aws-cdk-lib 2.258.0 emits cloud-assembly schema v54, which needs CDK CLI
# >= 2.1126.0 (see README "CLI compatibility").
aws-cdk-lib==2.267.0
constructs>=10.5.0,<11.0.0
# Used at synth time to parse the authored apis/openapi/openapi.yaml so it can
# be rendered (server URL substituted) and inlined into the AgentCore target.
PyYAML>=6.0,<7.0
boto3
# Shared, industry-agnostic CDK constructs (editable install from the monorepo root).
-e ../cdk_constructs